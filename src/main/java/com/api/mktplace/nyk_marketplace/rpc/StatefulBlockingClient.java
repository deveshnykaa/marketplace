package com.api.mktplace.nyk_marketplace.rpc;

/*
import com.api.mktplace.nyk_marketplace.dto.InventoryDTO;
import com.api.mktplace.nyk_marketplace.entity.Inventory;
import org.springframework.amqp.core.TopicExchange;
import org.springframework.amqp.rabbit.core.RabbitTemplate;
import org.springframework.scheduling.annotation.Scheduled;
import org.springframework.stereotype.Component;

@Component
public class StatefulBlockingClient {
    private final RabbitTemplate template;

    private final TopicExchange topicExchange;

    public StatefulBlockingClient(TopicExchange topicExchange, RabbitTemplate template){
        this.topicExchange=topicExchange;
        this.template=template;
    }

    @Scheduled(fixedDelay = 3000)
    public void send(){
        InventoryDTO inventoryDTO =
    }


}*/
